<div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Finance Manager', root_url, class: "navbar-brand" %>
    </div>
    <!-- Sidebar Menu Items - These collapse to the responsive navigation menu on small screens -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav side-nav">
        <% if user_signed_in? %>
            <li>
              <%= link_to t('title_wishes'), [current_user, :wishes] %>
            </li>
            <li>
              <%= link_to t('title_charges'), [current_user, :charges] %>
            </li>
            <li>
              <%= link_to t('title_calendar'), [current_user, :events] %>
            </li>
            <li>
              <%= link_to t('title_profile'), current_user %>
            </li>
            <li>
              <%= link_to t('title_sign'), destroy_user_session_path, id: "sign", :method => :delete %>
            </li>
        <% else %>
            <li>
            <button type="button" class=" fa fa-sign-in btn" id="login" data-toggle="modal" data-target="#myModal"><%= I18n.t('login') %></button>
            </li>
        <% end %>
      </ul>
    </div>
